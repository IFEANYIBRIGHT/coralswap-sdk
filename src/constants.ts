export const FEE_SCALE_BPS = 10000;
export const DEFAULT_SLIPPAGE_BPS = 50;
export const DEFAULT_DEADLINE_SECONDS = 300;
export const DEFAULT_MAX_RETRIES = 4;
export const DEFAULT_BASE_DELAY_MS = 250;
export const DEFAULT_MAX_DELAY_MS = 8_000;
export const DEFAULT_POLL_INTERVAL_MS = 1_000;
export const DEFAULT_RPC_TIMEOUT_MS = 30_000;
export const DEFAULT_STALENESS_SECONDS = 60 * 60;

export const REDACTED_MARKER = "[REDACTED]";

export const NATIVE_ASSET_SYMBOLS = ["XLM", "native"] as const;